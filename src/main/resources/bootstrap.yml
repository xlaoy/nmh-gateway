spring:
  application:
    name: nmh-gateway
  cloud:
    config:
      label: master
      name: xlaoy
      profile: dev
      username: config
      password: 123456
      discovery:
        enabled: true
        service-id: config
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery:123456@127.0.0.1:9920/eureka/
  instance:
    prefer-ip-address: true